name: 'job_market_pipeline'
version: '1.0.0'
profile: 'job_market_pipeline'

model-paths:    ["models"]
analysis-paths: ["analyses"]
test-paths:     ["tests"]
seed-paths:     ["seeds"]
macro-paths:    ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  job_market_pipeline:
    staging:
      +materialized: view
      +schema: STG
    intermediate:
      +materialized: view
      +schema: INT
    marts:
      +materialized: table
      +schema: MARTS
